# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Claude Code per-user overrides
.claude/settings.local.json

# Codex local/debug state
.codex-log/
.agents/checkpoints.log

# Local scratch — cloned repos for analysis, drafts, etc.
throwaway/

# Landing page / portfolio handoff — kept local, not tracked
landing/

# Local planning scratch — kept on disk, not tracked
ROADMAP.md

# Vendored MCP server — runtime deps are installed locally, never committed
vendor/**/node_modules/

# Obsidian local workspace state (vault note tracking is your call; cruft isn't)
memory/**/.obsidian/workspace*.json
memory/**/.obsidian/cache
memory/**/.trash/

# If you do NOT want generated artifacts checked in, uncomment:
# docs/briefs/
# docs/prd/
# docs/architecture/
# docs/stories/
# docs/adrs/
# docs/postmortems/
# docs/migrations/
# docs/data-models/
# REPOMAP.md
